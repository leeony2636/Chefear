# ⚙️ Orchestration

STT 모델 실행에 필요한 환경변수 로딩과 실행 보조 코드를 보관하는 폴더입니다.

## 📁 파일 안내

- `__init__.py`: Python 패키지 인식용 파일
- `db.py`: 환경변수 로딩 및 실행 환경 보조 코드

본 폴더는 핵심 STT 모델을 개발하는 영역이 아니라, `stt/` 폴더의 학습·추론 코드가 실행될 수 있도록 지원하는 보조 영역입니다.